apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pychart.fullname" . }}-test-connection"
  labels:
    {{- include "pychart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pychart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
